# Ruta de la base SQLite. En desarrollo es relativa a la carpeta prisma/
DATABASE_URL="file:./dev.db"

# En produccion (Dokploy) apuntar al volumen persistente:
# DATABASE_URL="file:/app/data/horely.db"
